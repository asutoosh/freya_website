# IP2Location.io API Configuration
# Get your free API key at: https://www.ip2location.io/sign-up
IP2LOCATION_API_KEY=

# Blocked Country Configuration
# Set the country code to block (default: IN for India)
# Use ISO 3166-1 alpha-2 country codes (e.g., IN, US, GB, CN, etc.)
BLOCKED_COUNTRY_CODE=IN

# Testing Configuration
# Set to the same value as BLOCKED_COUNTRY_CODE to bypass blocking during development
# For example, if you're in India and BLOCKED_COUNTRY_CODE=IN, set this to "IN" to bypass
# Remove or set to empty string in production
BYPASS_COUNTRY_CODE=IN

# Development Mode
# Set to "true" to disable ALL blocking features (timer, IP check, localStorage lock)
# This allows you to develop and test freely without restrictions
# IMPORTANT: Set to "false" or remove in production!
DEV_MODE=true
NEXT_PUBLIC_DEV_MODE=true
